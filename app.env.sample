# Docker Registry — environment variable reference
# Copy relevant lines to a .env file alongside docker-compose.yaml

# Timezone
TZ=America/New_York

# Registry UI title
REGISTRY_TITLE=Docker Registry

# Allow image deletion via UI
DELETE_IMAGES=false
